@{
    ViewData["Title"] = "Home Page";
}

@model tl2_tp10_2023_ezemrtz.ViewModels.ListarTareasViewModel

<h2> Lista de Tareas </h2>
<table class="table table-striped">
    <thead>
        <th>Id</th>
        <th>Id Tablero</th>
        <th>Nombre</th>
        <th>Descripcion</th>
        <th>Color</th>
        <th>Estado</th>
        <th>Id Usuario Asignado</th>
        <th>Modificar</th>
        <th>Eliminar</th>
    </thead>
    <tbody>
        @foreach(var tarea in Model.Tareas){
            <tr>
                <td>@tarea.Id</td>
                <td>@tarea.IdTablero</td>
                <td>@tarea.Nombre</td>
                <td>@tarea.Descripcion</td>
                <td>@tarea.Color</td>
                <td>@tarea.Estado</td>
                <td>@tarea.IdUsuarioAsignado</td>
                <td><a asp-controller="Tarea" asp-action="UpdateTarea" asp-route-id="@tarea.Id" class="btn btn-outline-primary">Modificar</a></td>
                <td><a asp-controller="Tarea" asp-action="DeleteTarea" asp-route-id="@tarea.Id" class="btn btn-outline-danger" onclick="return confirm('¿Estas seguro que quieres eliminar la tarea?')">Eliminar</a></td>
            </tr>
        }
    </tbody>
</table>