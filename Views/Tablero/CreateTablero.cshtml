@{
    ViewData["Title"] = "Tableros";
}
@model tl2_tp10_2023_ezemrtz.ViewModels.CrearTableroViewModel

<section class="section_forms">
    <form asp-controller="Tablero" asp-action="CreateTablero" class="formsMVC form-v1">
        <legend>NUEVO TABLERO</legend>
        <div class="form-group mb-3">
            <label asp-for="@Model.Nombre"></label>
            <input asp-for="@Model.Nombre" class="form-control" />
            <span asp-validation-for="@Model.Nombre" class="text-danger"></span>    
        </div>
        <div class="form-group mb-3">
            <label asp-for="@Model.Descripcion"></label>
            <input asp-for="@Model.Descripcion" class="form-control" />
            <span asp-validation-for="@Model.Descripcion" class="text-danger"></span>    
        </div>
        <div class="form-group mb-3">
            <input asp-for="@Model.IdUsuarioPropietario" class="form-control" hidden />
        </div>
        <div class="form-group mb-3 btn-box">
            <button type="submit" class="btn btn-primary">Enviar</button>
        </div>
    </form>
</section>

@section Scripts{<partial name="_ValidationScriptsPartial"/>}