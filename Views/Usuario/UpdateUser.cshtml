@model tl2_tp10_2023_ezemrtz.ViewModels.ModificarUsuarioViewModel

<section class="section_forms">
    <form asp-controller="Usuario" asp-action="UpdateUser" class="formsMVC">
        <input asp-for="@Model.Id" hidden>
        <div class="form-group mb-3">
            <label asp-for="@Model.Nombre">Nombre de usuario</label>
            <input asp-for="@Model.Nombre" class="form-control" />
        </div>
        <div class="form-group mb-3">
            <label asp-for="@Model.Contrasenia">Contaseña</label>
            <input asp-for="@Model.Contrasenia" class="form-control" type="password"/>
        </div>
        <div class="form-group mb-3">
            <label asp-for="@Model.Rol">Rol</label>
            <select asp-for="@Model.Rol" asp-items="@Html.GetEnumSelectList<NivelAcceso>()" class="form-select" aria-label="Default select example"></select>
        </div>
        <div class="form-group mb-3">
            <button type="submit" class="btn btn-primary">Enviar</button>
        </div>
    </form>
</section>